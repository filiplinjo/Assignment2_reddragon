// MQTT broker
var mosca = require('mosca')
var settings = {port: 8080}
var broker = new mosca.Server(settings)

// MongoDB
var mongc = require('mongodb').MongoClient
const MONGODB_URI= = "mongodb+srv://filip.h.eikenes@gmail.com:cloud98reddragon@cluster0.zhaw6.mongodb.net/myFirstDatabase?retryWrites=true&w=majority"; // Your connection string
const uri = process.env.MONGODB_URI;
const client = new mongc(uri, { useNewUrlParser: true, useUnifiedTopology: true });


async function run() {
  try {
    await client.connect();


broker.on('ready', ()=>{
 console.log('Broker is ready!')
})

broker.on('published', (packet)=>{
 message = packet.payload.toString()
 console.log(message)
 mongc.connect(url, (error, client)=>{
 var myCol = client.db('project1').collection('home') // DB name and collection name
 myCol.insertOne({
 message: message
 }, ()=>{
 console.log('Data is saved to MongoDB')
 client.close()
 })
 })
})

} finally {
    // Ensures that the client will close when you finish/error
   // await client.close();
  }
}
run().catch(console.dir);
